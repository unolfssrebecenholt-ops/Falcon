# Codex 本地规则

- 普通 Codex 聊天、代码编辑和补全，继续使用 `~/.codex/config.toml` 中配置的服务提供方。
- 当用户要求使用 image2 进行图片生成或编辑时，读取 `~/.codex/image2.toml`，并使用 `base_url` + `endpoint` 以及 OpenAI 风格的 Bearer 认证发起调用。
- 不要把普通 Codex 请求路由到 image2，也不要打印完整的 image2 API key。
- 本项目所有场景都始终使用 GPT-5.5 模型，包括 subagents、subprocesses、简单任务、委托任务和后台任务。除非用户明确覆盖这条规则，否则不要降级或切换到更小/更快的模型。
- 之后由 Codex 自主补充、整理或记录的项目规则，必须使用中文书写；除非用户明确要求保留英文原文，或者内容本身是代码标识、命令、路径、配置键、API 名称或专有名词。

## Falcon Agent 重启规则

- Falcon 正在重建为第一方本地内容运营 agent。
- Codex 只作为开发助手使用。不要设计依赖 Codex 对话作为运行时的产品采集流程。
- 不要重新引入已删除的外部 workflow-builder 采集路径，除非用户明确要求创建单独的历史恢复分支。
- 不要恢复旧采集适配器、旧 workflow 记录或已删除的采集文档作为兜底材料。
- 未来的采集路径应由 Falcon 自有浏览器采集器负责，包括平台适配器、标准化记录、本地资产存储、去重、分析、生成，以及人工确认后的执行预览。

## 浏览器控制规则

- Codex 做 Falcon 本地 UI、路由或导航验证时，可以使用 Codex 内置 Browser、命令行 Playwright/Chromium 截图与页面断言、`curl`、测试和日志检查；涉及视觉布局时，应优先使用能截图和交互的真实渲染环境，而不是只看 HTML。
- 禁止为了 Falcon 开发验证打开、激活或复用用户日常 Google Chrome；不要使用 Chrome 插件、Computer Use、`open -a "Google Chrome"`、macOS `open`、Python `webbrowser.open` 或其他会把用户日常浏览器切到前台的方式访问 `http://127.0.0.1:8765`。
- 如需要 Playwright/Chromium 渲染，应使用独立环境：临时 `user_data_dir` 或无持久 profile、显式设置桌面级 viewport、不要复用用户日常 Chrome profile；验证完成后必须主动关闭浏览器上下文和浏览器进程，临时 profile 按需清理。
- 允许使用 Codex 内置 Browser 检查 Falcon 页面、点击导航、查看弹窗、截图和评估样式；如果内置 Browser 无法提供足够 viewport 或截图质量，再改用独立 Playwright/Chromium 生成桌面级截图。
- 只有在必须使用用户真实登录态、Chrome 扩展、系统级交互，或 Codex 内置 Browser 与独立 Playwright 都无法复现的问题时，才允许请求改用用户可见的外部浏览器；必须先得到用户明确同意，再说明会打开哪个浏览器、访问哪个地址以及何时关闭。
- 如果涉及 UI 或视觉布局改动，并且需要通过浏览器查看效果，应设置接近最大化窗口的 viewport（例如桌面宽屏尺寸）后再截图或判断最终效果；不要用小 viewport 代表最终桌面效果。
- 进行前端页面编码、视觉重构或样式调整时，应在关键阶段把当前页面截图发送到会话框里，例如完成首版布局、完成主要样式、完成响应式/交互修正后各发一次；截图应覆盖整体页面状态，帮助及时确认视觉方向没有跑偏。

## 视觉验证体系规则

- 视觉验证不是检查某个 CSS 属性是否生效，而是验证用户在真实界面里能否清楚、稳定、低成本地完成任务。所有 UI 结论必须围绕真实用户任务、页面状态、数据内容和可见截图来判断。
- 结论证据优先级必须是：用户可见截图和局部放大图 > 真实浏览器中的交互观察 > 自动化断言和 DOM 几何测量 > CSS/模板代码意图。不能只因为代码写了某个布局属性、DOM 测量相等或测试通过，就断言视觉结果已经达成。
- 视觉验证规则必须实际执行后才能下结论。凡是回复“完成、已实现、已居中、已对齐、看起来正确、验证通过”这类结论前，必须先完成截图检查，并在内部或回复中形成明确的肉眼判断；如果只完成了代码修改或 DOM 测量，只能说“代码已调整”或“测量结果为...”，不能说视觉验收通过。
- 每次 UI 验证前必须先声明或在内部明确验证范围：改动页面、关键用户任务、主要组件、受影响导航入口、数据状态、目标 viewport、滚动位置和参照容器。范围不清时，先补足范围再下结论。
- 按风险选择验证深度：小型文案或颜色改动至少检查目标组件截图；组件布局改动至少检查组件局部、整页上下文和主要状态；整页重构至少检查桌面整页、关键局部、滚动位置、空/满数据、窄屏和主要交互；涉及导航、模板 include、路由上下文或提交动作时，必须同时做功能和路由验证。
- UI 设计、重构或明显视觉调整不能只在最终一次性发截图。必须在关键阶段把当前截图发到会话中，让用户及时确认方向没有跑偏；通常至少包括首版结构、主要视觉样式、关键交互/状态和最终验收四个阶段。若改动很小，可以说明为什么只提供目标组件截图。
- 截图证据至少分为三类：桌面级整页截图用于判断整体结构和信息层级；关键组件局部放大截图用于判断对齐、留白、可读性、视觉重心和文字溢出；交互状态截图用于判断 hover、focus、active、selected、disabled、loading、error、empty、expanded、modal、dropdown、toast 等状态。只看整页缩略图不能作为局部视觉结论。
- 如果用户指出红框、截图局部或某个具体组件，必须按用户标注区域建立参照容器，并做局部放大或裁切检查。最终回答必须按用户实际看到的区域下结论，不能把完整 DOM 容器、当前视口、滚动容器和截图裁切区域混为一谈。
- 做几何测量时必须同时检查目标元素是否被视口、滚动容器、截图边界、遮罩、固定栏、浮层、工具条或裁切框截断。若元素被截断，应分别报告完整 DOM 容器内结果和用户实际可见区域内结果。
- 判断视觉中心、视觉平衡、拥挤程度、好看与否时，必须以局部放大截图的肉眼观感为最终依据。几何中心、bounding box、上下左右 gap 相等只能说明数学位置，不等于视觉居中；图标大小、文字行数、字重、行高、空白面积、阴影、边框和周围内容都会改变视觉重心。
- 几何测量和肉眼截图结论冲突时，必须立即停止“已通过”表述，并明确写出冲突：例如“测量居中，但截图视觉偏上”。随后优先修正视觉问题或向用户说明仍未达成，不能用测量结果覆盖用户可见观感。
- 布局验收必须检查信息层级、网格结构、边距节奏、对齐关系、视觉重心、密度、分组、固定栏、滚动区域、层级遮挡和跨区域关系。不能只检查用户最初指出的一个属性，例如居中、颜色或字号。
- 文本验收必须检查标题层级、行长、换行、截断、长词、数字、标签、按钮文案、表单提示、错误信息和多语言/中英文混排。任何文本不得溢出容器、遮挡其他内容或在按钮/卡片内显得拥挤。
- 组件验收必须覆盖默认、hover、focus、active、selected、disabled、loading、empty、error 和权限受限状态。按钮、输入框、下拉、表格、卡片、弹窗、侧栏、工具栏、分页和危险操作必须使用一致的交互语汇。
- 表单和操作流验收必须覆盖创建、编辑、删除、取消、保存、重复提交、失败恢复、必填缺失、非法输入、长内容、批量操作和确认弹窗。影响下游处理的动作必须有明确反馈和可理解的状态变化。
- 数据边界验收必须至少考虑零数据、少量数据、典型数据、大量数据、长标题、长正文、缺失字段、异常状态和混合状态。不能只用理想短文本样本判断 UI 合理。
- 响应式验收必须覆盖项目实际使用的主要断点。桌面优先项目至少检查接近最大化桌面 viewport、较窄桌面/平板宽度和必要的移动宽度；每个断点都要检查导航、主要内容、表单、弹窗、滚动和文字是否可用。
- 可访问性验收必须检查正文和控件对比度、键盘可达性、焦点可见性、语义标签、表单 label、按钮可辨识名称、状态不只依赖颜色、减少动态效果、触控/点击目标尺寸和禁用状态表达。
- 视觉风格验收必须检查是否符合产品调性和已有设计系统：颜色数量、字号层级、圆角、边框、阴影、图标、间距、卡片使用、表格密度、空状态和动效都应与项目气质一致。不得为了局部“好看”破坏整体一致性。
- 性能和稳定性验收必须关注首屏加载、滚动流畅度、布局抖动、图片/字体加载、动画成本、控制台错误、网络失败状态和重复渲染。视觉正确但明显卡顿或闪烁，不能算完成。
- 回归验收必须检查受影响页面的相邻入口和共享组件。对 Falcon，本地 UI 改动后还必须遵守“UI 模板与导航验证规则”中的主要目录入口检查，避免只验证当前页面。
- 发现视觉问题后，不要急于继续改代码。先复盘验证口径：用户任务是否正确、参照容器是否正确、截图是否完整、局部是否放大、元素是否被裁切、状态是否一致、数据是否代表真实场景。确认问题性质后再修改。
- 给用户发送截图前，应先自查截图是否真实展示了要说明的问题：目标区域不能太小，不能被底部工具条、浏览器 UI、遮罩或截图裁切遮住，不能把局部判断建立在缩略图上。如果截图本身会误导，应发送局部放大图或说明截图局限。
- 发送阶段性截图或最终截图时，必须附带一句基于截图的视觉判断，而不只是贴图或贴测量数据。判断应直接说明是否达成、哪里仍不舒服、下一步要改什么；如果截图里仍有明显问题，必须主动承认问题，不要等待用户指出。
- 在最终回复中陈述 UI 结果时，必须区分“代码已调整”“浏览器已渲染”“截图里看起来正确”“自动化检查通过”“仍需用户确认”这几种状态。没有经过截图或真实浏览器验证时，只能说代码已调整，不能说视觉结果已经达成。
- 这套视觉验证规则可复制到其他项目的 `AGENTS.md`。如果其他项目没有 Falcon 的浏览器限制，仍应保留“真实渲染、整页加局部、用户任务优先、可见区域优先、截图观感优先于代码意图和 DOM 测量”的核心原则。

## UI 模板与导航验证规则

- 每次改动 Jinja 模板、左侧目录、页面入口、弹窗或可复用表单组件后，必须检查所有会 include 该组件的路由是否传入完整上下文；尤其关注 `defaults`、`platforms`、`profile_options`、筛选状态、统计数据和弹窗所需变量。
- 可复用模板不能假设只会被单一路由使用。新增 include 时，应在模板内提供安全 fallback，或在所有入口显式传入变量，避免点击目录后出现 `jinja2.exceptions.UndefinedError`。
- UI 改动后不能只验证当前页面。至少要请求或点击左侧目录中的主要入口：仪表盘、采集首页、任务队列、账号管理、环境自检、分析首页、分析样本、人工复核、执行首页、触达任务和设置，确认没有 `500 Internal Server Error`。
- 如果改动了 Python 路由上下文、模板 include 关系或静态资源版本，必须重启当前 `runtime/falcon-web.pid` 对应的 Falcon Web 服务，再通过 Codex 内置 Browser、独立 Playwright、`curl`、测试或日志请求 `http://127.0.0.1:8765` 验证；不要只在临时端口验证后就结束。
- 遇到目录点击报错时，先看 `runtime/falcon-web.log` 的最新 traceback，再修模板上下文或重启服务；不要只从页面上的 `Internal Server Error` 猜测原因。
