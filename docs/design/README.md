# Falcon Design References

This directory intentionally keeps only the current design reference:

- `falcon-layout-redesign-v1/`: latest full workbench prototype, including dashboard, collection, analysis, review, execution, tasks, and desktop/mobile screenshots.

Older one-off prototypes and historical screenshot batches were removed to keep the repository small and easier to navigate. Use Git history if an older visual direction is needed for comparison.
