# Falcon Design References

Production Web is now the source of truth for the current Falcon workbench design:

- `falcon/web/templates/`
- `falcon/web/static/app.css`

Historical HTML prototypes, one-off color previews, and screenshot batches were removed to keep the repository small and easier to navigate. Use Git history if an older visual direction is needed for comparison.
