# 当前影刀主流程截图记录

本文件记录用户在影刀中已经配置的主流程截图内容。只记录截图中可见的指令、变量和元素名，不补充未出现的元素。

## 截图中已确认的流程段

### 关键词列表初始化

| 步骤 | 指令 | 截图中可见配置 |
| --- | --- | --- |
| 1 | `新建列表` | 将列表对象保存到 `keywords` |
| 2 | `列表插入一项` | 在 `keywords` 末尾追加 `小红书封面` |
| 3 | `列表插入一项` | 在 `keywords` 末尾追加 `小红书标题图` |
| 4 | `列表插入一项` | 在 `keywords` 末尾追加 `笔记没人看` |
| 5 | `列表插入一项` | 在 `keywords` 末尾追加 `AI头像` |
| 6 | `列表插入一项` | 在 `keywords` 末尾追加 `活动海报` |

### 搜索循环

| 步骤 | 指令 | 截图中可见配置 |
| --- | --- | --- |
| 7 | `ForEach列表循环` | 对 `keywords` 中每一项循环，当前项保存到 `current_keyword` |
| 8 | `打开网页` | 在影刀浏览器中新建标签页 `https://www.xiaohongshu.com/`，网页对象保存到 `web_page` |
| 9 | `等待网页加载完成` | 等待网页 `web_page` 加载完成，等待时间 `20` 秒 |
| 10 | `点击元素(web)` | 在 `web_page` 中单击网页元素 `输入框_search-input` |
| 11 | `填写输入框(web)` | 在 `web_page` 的 `输入框_search-input` 中输入 `current_keyword` |
| 12 | `点击元素(web)` | 在 `web_page` 中单击网页元素 `块元素_search-icon` |
| 13 | `等待网页加载完成` | 等待网页 `web_page` 加载完成，等待时间 `20` 秒 |

### 搜索结果加载

| 步骤 | 指令 | 截图中可见配置 |
| --- | --- | --- |
| 14 | `鼠标滚动网页` | 在 `web_page` 中滚动到底部，滚动效果为平滑滚动 |
| 15 | `等待` | 等待 `3` 秒 |
| 16 | `鼠标滚动网页` | 在 `web_page` 中滚动到底部，滚动效果为平滑滚动 |
| 17 | `等待` | 等待 `4` 秒 |
| 18 | `鼠标滚动网页` | 在 `web_page` 中滚动到底部，滚动效果为平滑滚动 |
| 19 | `等待` | 等待 `4` 秒 |
| 20 | `鼠标滚动网页` | 在 `web_page` 中滚动到底部，滚动效果为平滑滚动 |
| 21 | `等待` | 等待 `4` 秒 |
| 22 | `鼠标滚动网页` | 在 `web_page` 中滚动到底部，滚动效果为平滑滚动 |
| 23 | `循环结束标记` | 表示一个循环区域的结尾 |

## 已确认变量

| 变量 | 来源 | 用途 |
| --- | --- | --- |
| `keywords` | `新建列表` | 存储搜索关键词 |
| `current_keyword` | `ForEach列表循环` | 当前搜索关键词 |
| `web_page` | `打开网页` | 当前小红书网页对象 |

## 已确认网页元素

| 元素名 | 来源 | 当前用途 | Falcon 映射 |
| --- | --- | --- | --- |
| `输入框_search-input` | 主流程截图 | 搜索框点击与输入 | `search_input` |
| `块元素_search-icon` | 主流程截图 | 触发搜索 | `search_button` |

## 当前流程状态判断

已完成：

- 关键词列表初始化。
- 按关键词循环。
- 打开小红书。
- 输入关键词并点击搜索。
- 低频滚动加载结果。

尚未完成：

- 获取搜索结果帖子卡片相似元素。
- 循环帖子卡片。
- 从帖子卡片提取标题、摘要、链接、时间。
- 写入 Falcon CSV 表头和数据行。
- 导出 CSV。
- 可选评论采集。

## 下一步需要的元素截图

请优先补充搜索结果页的影刀元素库截图：

1. 搜索结果帖子卡片相似元素。
2. 卡片标题元素。
3. 卡片链接元素或可获取链接的卡片/封面元素。
4. 卡片摘要元素，如果搜索结果页有。
5. 卡片时间元素，如果搜索结果页有。
