[project]
name = "falcon-demand-radar"
version = "0.1.0"
description = "Local social demand radar and AI outreach task box for AI出图助手."
requires-python = ">=3.9"
dependencies = []

[project.scripts]
falcon = "falcon.cli:main"
